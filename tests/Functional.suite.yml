actor: FunctionalTester
modules:
  enabled:
    - DataFactory:
        depends: Doctrine
        cleanup: true
    - \Support\Helper\Factories
    - Asserts
    - Symfony:
        app_path: 'src'
        environment: 'test'
    - Doctrine:
        depends: Symfony
        cleanup: true
